When building, use 8 cores. The build should go in ../build If you are not able to put it there, tell the user and stop.

When testing things, do not EVER load a model yourself. If testing needs a model to be loaded, ask the user to do it.

Logs are in /home/doug/llamacpp_launcher/logs